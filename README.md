## GitHub Page Deployment Project

Static site with CI/CD pipeline and artifact-based deployment using GitHub Actions.

**Workflow:**
- Build site with Hugo
- Run basic checks (index + links)
- Upload `public/` as artifact
- Deploy to GitHub Pages (no rebuild)

Focused on clean CI/CD with artifact-based deployment.
